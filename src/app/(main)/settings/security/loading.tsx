/**
 * @project AncestorTree
 * @file src/app/(main)/settings/security/loading.tsx
 * @description Loading skeleton for the (main)/settings/security route
 * @version 2.0.0
 * @updated 2026-08-09
 */

import { PageSkeleton } from '@components/shared';

export default function SettingsSecurityLoading() {
  return <PageSkeleton variant="form" />;
}
