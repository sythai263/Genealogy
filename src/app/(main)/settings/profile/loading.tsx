/**
 * @project AncestorTree
 * @file src/app/(main)/settings/profile/loading.tsx
 * @description Loading skeleton for the (main)/settings/profile route
 * @version 2.0.0
 * @updated 2026-08-09
 */

import { PageSkeleton } from '@components/shared';

export default function SettingsProfileLoading() {
  return <PageSkeleton variant="form" />;
}
